1. replace Webhook URl in app.py with URL form Discord Webhook (Discord channel settings -> integrations -> WebHooks -> copy WebHook-URL)

2. run this project with
```SQL
docker-compose up --build
```
